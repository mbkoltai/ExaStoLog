function update_matrix =fcn_truthtable(nodes)

n=numel(nodes); list_binary_states=rem(floor((0:((2^n)-1)).'*pow2(0:-1:-n+1)),2);
size(list_binary_states)
disp(nodes)

update_matrix = [function update_matrix =fcn_truthtable(nodes)

n=numel(nodes); list_binary_states=rem(floor((0:((2^n)-1)).'*pow2(0:-1:-n+1)),2);
size(list_binary_states)
disp(nodes)

update_matrix = [